{
  "name": "janis-dashboards-functions",
  "description": "Cloud Functions: proxy seguro al data lake de JANIS (ClickHouse) con auth por dominio",
  "version": "0.1.0",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@clickhouse/client": "^1.7.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
